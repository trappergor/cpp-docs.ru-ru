---
title: Неустранимая ошибка C1083
ms.date: 09/01/2017
f1_keywords:
- C1083
helpviewer_keywords:
- C1083
ms.assetid: 97e52df3-e79c-4f85-8f1e-bbd1057d55e7
ms.openlocfilehash: 522bc4a36be59d4e2c9425e50b1238eb9f4aba61
ms.sourcegitcommit: 8105b7003b89b73b4359644ff4281e1595352dda
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 03/14/2019
ms.locfileid: "57822201"
---
# <a name="fatal-error-c1083"></a>Неустранимая ошибка C1083

> Не удается открыть *filetype* файл: "*файл*": *сообщения*

Компилятор создает ошибку C1083, когда не удается найти файл, необходимым. Существует несколько возможных причин этой ошибки. Выполняется поиск неверные include путь или отсутствует или неправильными именами файлов заголовков являются наиболее распространенных причин, но другие типы файлов и проблемы также могут вызвать C1083. Ниже приведены некоторые из распространенных причин, почему компилятор создает эту ошибку.

## <a name="the-specified-file-name-is-wrong"></a>Указано неверное имя файла

При вводе имени файла допущена ошибка. Например, примененная к объекту директива

`#include <algorithm.h>`

может не найти файл, который вы хотели. Большинство файлов заголовка стандартной библиотеки C++ нет .h расширение имени файла. \<Алгоритм > заголовок не будут найдены в этом `#include` директива. Чтобы устранить эту проблему, убедитесь, что введено правильное имя файла, как в следующем примере:

`#include <algorithm>`

Некоторые заголовки библиотеки времени выполнения C расположены в подкаталоге стандартного каталога включения. Например, чтобы включить sys/types.h, необходимо включить имя подкаталога sys в `#include` директивы:

`#include <sys/types.h>`

## <a name="the-file-is-not-included-in-the-include-search-path"></a>Файл не включается в путь поиска включаемых

Компилятору не удается найти файл, используя правила поиска, которые указаны в директиве `#include` или `#import`. Например, когда имя файла заголовка заключено в кавычки,

`#include "myincludefile.h"`

Это указывает компилятору поиск файла в тот же каталог, содержащий исходный файл, сначала, а затем в других расположениях, указанных средой сборки. Если кавычки содержат абсолютный путь, компилятор выполняет поиск файла только в этом расположении. Если кавычки содержат относительный путь, компилятор выполняет поиск файла в каталоге относительно исходного каталога.

Если имя заключено в угловые скобки

`#include <stdio.h>`

Компилятор следует пути поиска, определенный средой сборки, **/I** параметр компилятора **/X** параметра компилятора и **INCLUDE** переменной среды. Дополнительные сведения, включая подробные сведения о порядке поиска, используемый для поиска файла, см. в разделе [#include-директива (C/C++)](../../preprocessor/hash-include-directive-c-cpp.md) и [директива #import](../../preprocessor/hash-import-directive-cpp.md).

Если ваши файлы include, хранятся в другом каталоге относительно исходного каталога, и использовать относительный путь в вашей директивы #include, вместо угловых скобок необходимо использовать двойные кавычки. Например если вашей myheader.h файл заголовка находится в подкаталоге с именем заголовки источники проекта, в этом примере не удается найти файл и вызывает C1083:

`#include <headers\myheader.h>`

но этот пример работает:

`#include "headers\myheader.h"`

Относительные пути могут также использоваться с каталогами на пути поиска включаемых. Если добавить каталог **INCLUDE** переменной среды или вашей **каталоги включаемых файлов** путь в Visual Studio, следует добавлять в пути директив include. Например, если ваш заголовка находится в каталоге \path\example\headers\myheader.h и добавьте \path\example\headers\ для вашей **каталоги включаемых файлов** путь в Visual Studio, а `#include` директива ссылается на файл с именем

`#include <headers\myheader.h>`

затем файл не найден. Используйте правильный путь относительно каталога, указанного в пути поиска включаемых. В этом примере можно изменить путь к \path\example\, или удалить сегмент пути headers\ из `#include` директива.

## <a name="third-party-library-issues-and-vcpkg"></a>Проблемы стороннюю библиотеку и Vcpkg

Если вы видите эту ошибку при попытке настроить стороннюю библиотеку как часть сборки, рассмотрите возможность использования [Vcpkg](../../vcpkg.md), диспетчер пакетов C++ Visual, чтобы установить и выполнить сборку библиотеки. Vcpkg поддерживает большого и постоянно растущего [список библиотек сторонних](https://github.com/Microsoft/vcpkg/tree/master/ports)и задает все свойства конфигурации и зависимости, необходимые для успешной сборки как часть проекта. Дополнительные сведения см. в разделе связанных [блог о Visual C++](https://blogs.msdn.microsoft.com/vcblog/2016/09/19/vcpkg-a-tool-to-acquire-and-build-c-open-source-libraries-on-windows/) блога.

## <a name="the-file-is-in-your-project-but-not-the-include-search-path"></a>Файл находится в проекте, но не в путь поиска включаемых

Даже тогда, когда файлы заголовка перечислены в **обозревателе решений** как часть проекта, файлы являются только найденная компилятором, когда они ссылаются `#include` или `#import` директив источника файла и находятся в папке путь поиска включаемых файлов. Для различных типов построений могут использоваться разные пути поиска. **/X** параметр компилятора можно использовать для исключения каталогов из пути поиска включаемых. Это позволяет использовать для разных построений разные файлы include, которые имеют одно имя, но расположены в разных каталогах. Таким образом создается альтернатива условной компиляции с помощью команд препроцессора. Дополнительные сведения о **/X** параметр компилятора см. в разделе [/X (отклонение стандартных путей включения)](../../build/reference/x-ignore-standard-include-paths.md).

Чтобы устранить эту проблему, исправьте путь, используемый компилятором для поиска включаемого или импортируемого файла. Новое значение по умолчанию проект использует пути поиска. Вам может потребоваться изменить путь для добавления в каталог проекта. При компиляции в командной строке, добавьте путь к **INCLUDE** переменной среды или **/I** параметр компилятора, чтобы указать путь к файлу.

Чтобы задать путь к каталогу включаемых в Visual Studio, откройте проект **страницы свойств** диалоговое окно. Выберите **каталоги VC ++** под **свойства конфигурации** в левой области, а затем изменять **каталоги включаемых файлов** свойство. Дополнительные сведения о пользователя и для каждого проекта каталоги поиска компилятора в Visual Studio, см. в разделе [страница свойств каталогов VC ++](../../build/reference/vcpp-directories-property-page.md). Дополнительные сведения о **/I** параметр компилятора см. в разделе [/I (Дополнительные каталоги включаемых файлов)](../../build/reference/i-additional-include-directories.md).

## <a name="the-command-line-include-or-lib-environment-is-not-set"></a>Командная строка INCLUDE или LIB среды не заданы

Если компилятор вызывается из командной строки, для указания путей поиска часто используются переменные среды. Если в путь поиска, описываемое **INCLUDE** или **LIB** переменную среды задано неправильно, может возникать ошибка C1083. Настоятельно рекомендуется с помощью ярлыка Командная строка разработчика для установки базовой среды для командной строки сборки. Дополнительные сведения см. в разделе [построения C/C++ в командной строке](../../build/building-on-the-command-line.md). Дополнительные сведения об использовании переменных среды см. в разделе [как: Использование переменных среды в построении](/visualstudio/msbuild/how-to-use-environment-variables-in-a-build).

## <a name="the-file-may-be-locked-or-in-use"></a>Файл может быть заблокирован или используется

Если вы используете другую программу для изменения или доступа к файлу, он может иметь файл заблокирован. Закройте файл в другой программе. Иногда другие программы может быть Visual Studio, если вы используете параметры параллельной компиляции. Если отключение параметра параллельной сборки делает ошибку от него, то это проблема. Другие системы параллельной сборки могут также иметь эту проблему. Будьте внимательны и задайте файл зависимостей проекта, чтобы порядок сборки указано правильно. В некоторых случаях рекомендуется создать промежуточный проект, чтобы принудительно порядок зависимостей сборки для общего файла, который можно скомпилировать в нескольких проектах. Иногда антивирусные программы временно заблокировать недавно измененных файлов для сканирования. Если это возможно рекомендуется, исключить каталоги сборки проекта из антивирусной программы.

## <a name="the-wrong-version-of-a-file-name-is-included"></a>Включена неправильная версия имени файла

Ошибка C1083 также может указывать на включение неверной версии файла. Например, построение может включать неверную версию файла, содержащего директиву `#include` для файла заголовка, который не предназначен для данного построения. Например, некоторые файлы может применяться только к x86 сборки, или для отладочных сборок. Если файл заголовка не найден, компилятор выдает ошибку C1083. Для исправления этой проблемы необходимо использовать правильный файл, а не добавлять в построение файл заголовка или каталог.

## <a name="the-precompiled-headers-are-not-yet-precompiled"></a>Предкомпилированные заголовки еще не скомпилированы

Если в проекте настроено использование предкомпилированных заголовков, то для обеспечения возможности компиляции файлов, которые используют содержимое заголовков, необходимо создать соответствующие PCH-файлы. Например автоматически создается файл stdafx.cpp в каталоге проекта для новых проектов. Сначала необходимо скомпилировать этот файл, чтобы создать предкомпилированные файлы заголовков. В типичном процессе построения это выполняется автоматически. Дополнительные сведения см. в разделе [Создание файлов предкомпилированных заголовков](../../build/creating-precompiled-header-files.md).

## <a name="additional-causes"></a>Дополнительные причины

- Установки пакета SDK или стороннюю библиотеку, но не открыто новое окно командной строки разработчика после пакет SDK или библиотека устанавливается. Если пакет SDK или библиотека добавляет файлы для **INCLUDE** пути, может потребоваться открыть новое окно командной строки разработчика применить эти изменения переменных среды.

- В файле используется управляемый код, но параметр компилятора **/CLR** не указан. Дополнительные сведения см. в разделе [/clr (компиляция CLR)](../../build/reference/clr-common-language-runtime-compilation.md).

- Файл скомпилирован с помощью другого **/ analyze** значения параметра компилятора, чем используется для предварительной компиляции заголовков. При предварительной компиляции заголовков для проекта, все они должны использовать одинаковые **/ analyze** параметры. Дополнительные сведения см. в разделе [/analyze (анализ кода)](../../build/reference/analyze-code-analysis.md).

- Файл или каталог был создан с помощью системы Windows для Linux и включен учет регистра для-directory указанного варианта файла или путь не совпадает по регистру пути или файлу на диске.

- Файл, каталог или диск доступен только для чтения.

- Visual Studio или программы командной строки нет достаточных полномочий на чтение файла или каталога. Это может произойти, например, когда файлы проекта имеют разных владельцев, чем процесс под управлением Visual Studio или программы командной строки. Иногда можно устранить эту проблему, запустив Visual Studio или командной строки разработчика от имени администратора.

- Недостаточно дескрипторов файлов. Закройте часть приложений и повторите компиляцию. Эта ситуация маловероятна в обычных условиях. Однако она может возникать при построении крупных проектов на компьютере с ограниченной физической памятью.

## <a name="example"></a>Пример

В следующем примере возникает ошибка C1083 при заголовочный файл `"test.h"` не существует в исходном каталоге или на пути поиска включаемых.

```cpp
// C1083.cpp
// compile with: /c
#include "test.h"   // C1083 test.h does not exist
#include "stdio.h"  // OK
```

Сведения о построении проектов C/C++ в интегрированной среде разработки или в командной строке и сведения об установке переменных среды, см. в разделе [проектов и систем сборки](../../build/projects-and-build-systems-cpp.md).

## <a name="see-also"></a>См. также

- [Свойства MSBuild](/visualstudio/msbuild/msbuild-properties)
