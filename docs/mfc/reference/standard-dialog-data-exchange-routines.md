---
title: "Процедуры обмена данными стандартное диалоговое окно | Документы Microsoft"
ms.custom: 
ms.date: 11/04/2016
ms.reviewer: 
ms.suite: 
ms.technology:
- devlang-cpp
ms.tgt_pltfrm: 
ms.topic: article
dev_langs:
- C++
helpviewer_keywords:
- standard dialog, data exchange routines
ms.assetid: c6adb7f3-f9af-4cc5-a9ea-315c5b60ad1a
caps.latest.revision: 13
author: mikeblome
ms.author: mblome
manager: ghogen
translation.priority.ht:
- cs-cz
- de-de
- es-es
- fr-fr
- it-it
- ja-jp
- ko-kr
- pl-pl
- pt-br
- ru-ru
- tr-tr
- zh-cn
- zh-tw
translationtype: Machine Translation
ms.sourcegitcommit: 17a158366f94d27b7a46917282425d652e6b9042
ms.openlocfilehash: 15bd88000a7a035ed416b7e1c0640488039079ab
ms.lasthandoff: 02/24/2017

---
# <a name="standard-dialog-data-exchange-routines"></a>Стандартные программы обмена данных диалоговых окон
В этом разделе перечислены процедуры exchange (DDX) данных стандартного диалогового окна, используемые для общих элементов управления диалоговых окон MFC.  
  
> [!NOTE]
>  Процедуры обмена данными стандартное диалоговое окно, определяются в afxdd_.h файла заголовка. Тем не менее приложения должны включать afxwin.h.  
  
### <a name="ddx-functions"></a>Функции DDX  
  
|||  
|-|-|  
|[DDX_CBIndex](#ddx_cbindex)|Инициализирует и возвращает индекс выбранного элемента управления полем со списком.|  
|[DDX_CBString](#ddx_cbstring)|Инициализирует или получает текущее содержимое поля изменить поле со списком.|  
|[DDX_CBStringExact](#ddx_cbstringexact)|Инициализирует или получает текущее содержимое поля изменить поле со списком.|  
|[DDX_Check](#ddx_check)|Инициализирует или получает текущее состояние управления "флажок".|  
|[DDX_Control](#ddx_control)|Подклассы данного элемента управления в диалоговое окно.|  
|[DDX_DateTimeCtrl](#ddx_datetimectrl)|Инициализирует или получает данные даты и времени элемент управления выбора даты и времени.|  
|[DDX_IPAddress](#ddx_ipaddress)|Инициализирует или получает текущее значение контроль IP-адресов.|  
|[DDX_LBIndex](#ddx_lbindex)|Инициализирует и возвращает индекс выбранного элемента управления "список".|  
|[DDX_LBString](#ddx_lbstring)|Инициализирует или получает текущее выделение в элемент управления списком.|  
|[DDX_LBStringExact](#ddx_lbstringexact)|Инициализирует или получает текущее выделение в элемент управления списком.|  
|[DDX_MonthCalCtrl](#ddx_monthcalctrl)|Инициализирует или получает текущее значение элемента управления календаря на месяц.|  
|[DDX_Radio](#ddx_radio)|Инициализирует и возвращает отсчитываемый от нуля индекс элемента управления переключателя, находящегося в группе управления переключателя.|  
|[DDX_Scroll](#ddx_scroll)|Инициализирует или получает текущую позицию прокрутки элемента управления thumb.|  
|[DDX_Slider](#ddx_slider)|Инициализирует или получает текущее положение ползунка элемента управления thumb.|  
|[DDX_Text](#ddx_text)|Инициализирует или получает текущее значение элемента управления.|  
  
##  <a name="a-nameddxcbindexa--ddxcbindex"></a><a name="ddx_cbindex"></a>DDX_CBIndex  
 `DDX_CBIndex` Функция управляет передачей `int` данные между поле со списком в диалоговом окне, форму, представление или объект элемента управления представления и `int` диалоговое окно, представление формы или объект элемента управления представления элемента данных.  
  
```  
void AFXAPI DDX_CBIndex(
    CDataExchange* pDX,  
    int nIDC,  
    int& index);  
```  
  
### <a name="parameters"></a>Параметры  
 `pDX`  
 Указатель на объект `CDataExchange` . Структура предоставляет этот объект для формирования контекста обмена данными, включая его направление.  
  
 `nIDC`  
 Идентификатор ресурса списком, связанный со свойством элемента управления.  
  
 *Индекс*  
 Ссылка на переменную-член диалоговое окно, представление формы или объект элемента управления представления обмене данными.  
  
### <a name="remarks"></a>Примечания  
 Когда `DDX_CBIndex` вызывается, *индекс* присваивается индекс текущего выделенного фрагмента поле со списком. Если элемент не выбран, *индекс* имеет значение 0.  
  
 Дополнительные сведения об DDX см. в разделе [обмен данными диалоговых окон и проверка](../../mfc/dialog-data-exchange-and-validation.md).  
  
### <a name="requirements"></a>Требования  
  **Заголовок** afxdd_.h  
  
##  <a name="a-nameddxcbstringa--ddxcbstring"></a><a name="ddx_cbstring"></a>DDX_CBString  
 `DDX_CBString` Функция управляет передачей `CString` данные между редактируемый элемент управления поля со списком в диалоговом окне, форму, представление или объект элемента управления представления и `CString` диалоговое окно, представление формы или объект элемента управления представления элемента данных.  
  
```  
void AFXAPI DDX_CBString(
    CDataExchange* pDX,  
    int nIDC,  
    CString& value);  
```  
  
### <a name="parameters"></a>Параметры  
 `pDX`  
 Указатель на объект `CDataExchange` . Структура предоставляет этот объект для формирования контекста обмена данными, включая его направление.  
  
 `nIDC`  
 Идентификатор ресурса списком, связанный со свойством элемента управления.  
  
 *value*  
 Ссылка на переменную-член диалоговое окно, представление формы или объект элемента управления представления обмене данными.  
  
### <a name="remarks"></a>Примечания  
 Когда `DDX_CBString` вызывается, *значение* присваивается текущее выделение поле со списком. Если элемент не выбран, *значение* задана строка нулевой длины.  
  
> [!NOTE]
>  Если поле со списком представляет собой раскрывающийся список, значение обмен ограничено до 255 символов.  
  
 Дополнительные сведения об DDX см. в разделе [обмен данными диалоговых окон и проверка](../../mfc/dialog-data-exchange-and-validation.md).  
  
### <a name="requirements"></a>Требования  
  **Заголовок** afxdd_.h  
  
##  <a name="a-nameddxcbstringexacta--ddxcbstringexact"></a><a name="ddx_cbstringexact"></a>DDX_CBStringExact  
 `DDX_CBStringExact` Функция управляет передачей `CString` данные между редактируемый элемент управления поля со списком в диалоговом окне, форму, представление или объект элемента управления представления и `CString` диалоговое окно, представление формы или объект элемента управления представления элемента данных.  
  
```  
void AFXAPI DDX_CBStringExact(
    CDataExchange* pDX,  
    int nIDC,  
    CString& value);  
```  
  
### <a name="parameters"></a>Параметры  
 `pDX`  
 Указатель на объект `CDataExchange` . Структура предоставляет этот объект для формирования контекста обмена данными, включая его направление.  
  
 `nIDC`  
 Идентификатор ресурса списком, связанный со свойством элемента управления.  
  
 *value*  
 Ссылка на переменную-член диалоговое окно, представление формы или объект элемента управления представления обмене данными.  
  
### <a name="remarks"></a>Примечания  
 Когда `DDX_CBStringExact` вызывается, *значение* присваивается текущее выделение поле со списком. Если элемент не выбран, *значение* задана строка нулевой длины.  
  
> [!NOTE]
>  Если поле со списком представляет собой раскрывающийся список, значение обмен ограничено до 255 символов.  
  
 Дополнительные сведения об DDX см. в разделе [обмен данными диалоговых окон и проверка](../../mfc/dialog-data-exchange-and-validation.md).  
  
### <a name="requirements"></a>Требования  
  **Заголовок** afxdd_.h  
  
##  <a name="a-nameddxchecka--ddxcheck"></a><a name="ddx_check"></a>DDX_Check  
 `DDX_Check` Функция управляет передачей `int` данные между управления "флажок" в диалоговом окне, форму, представление или объект элемента управления представления и `int` диалоговое окно, представление формы или объект элемента управления представления элемента данных.  
  
```  
void AFXAPI DDX_Check(
    CDataExchange* pDX,  
    int nIDC,  
    int& value);  
```  
  
### <a name="parameters"></a>Параметры  
 `pDX`  
 Указатель на объект `CDataExchange` . Структура предоставляет этот объект для формирования контекста обмена данными, включая его направление.  
  
 `nIDC`  
 Идентификатор ресурса управления "флажок", связанный со свойством элемента управления.  
  
 *value*  
 Ссылка на переменную-член диалоговое окно, представление формы или объект элемента управления представления обмене данными.  
  
### <a name="remarks"></a>Примечания  
 Когда `DDX_Check` вызывается, *значение* присваивается текущее состояние управления "флажок". Список значений возможные состояния, в разделе [BM_GETCHECK](http://msdn.microsoft.com/library/windows/desktop/bb775986) в [!INCLUDE[winSDK](../../atl/includes/winsdk_md.md)] *.*  
  
 Дополнительные сведения об DDX см. в разделе [обмен данными диалоговых окон и проверка](../../mfc/dialog-data-exchange-and-validation.md).  
  
### <a name="requirements"></a>Требования  
  **Заголовок** afxdd_.h  
  
##  <a name="a-nameddxcontrola--ddxcontrol"></a><a name="ddx_control"></a>DDX_Control  
 `DDX_Control` Функции управления, определяемое подклассов `nIDC`, диалоговое окно, представление формы или объект элемента управления представления.  
  
```  
void AFXAPI DDX_Control(
    CDataExchange* pDX,  
    int nIDC,  
    CWnd& rControl);  
```  
  
### <a name="parameters"></a>Параметры  
 `pDX`  
 Указатель на [CDataExchange](../../mfc/reference/cdataexchange-class.md) объекта.  
  
 `nIDC`  
 Идентификатор ресурса для быть подклассом элемента управления.  
  
 *rControl*  
 Ссылка на переменную-член диалоговое окно, представление формы или объект элемента управления представления, связанные с указанным элементом управления.  
  
### <a name="remarks"></a>Примечания  
 `pDX` Объекта, предоставляемого платформой при `DoDataExchange` вызове функции. Таким образом `DDX_Control` должен вызываться только в пределах переопределение метода `DoDataExchange`.  
  
 Дополнительные сведения об DDX см. в разделе [обмен данными диалоговых окон и проверка](../../mfc/dialog-data-exchange-and-validation.md).  
  
### <a name="requirements"></a>Требования  
  **Заголовок** afxdd_.h  
  
##  <a name="a-nameddxdatetimectrla--ddxdatetimectrl"></a><a name="ddx_datetimectrl"></a>DDX_DateTimeCtrl  
 `DDX_DateTimeCtrl` Функция управляет передачей данных даты или времени между элемент выбора даты и времени ( [CDateTimeCtrl](../../mfc/reference/cdatetimectrl-class.md)) в объект диалогового окна формы или представления и либо [CTime](../../atl-mfc-shared/reference/ctime-class.md) или [COleDateTime](../../atl-mfc-shared/reference/coledatetime-class.md) элемент данных объекта представление формы или диалогового окна.  
  
```  
void AFXAPI DDX_DateTimeCtrl(
    CDataExchange* pDX,  
    int nIDC,  
    CTime& value);

void AFXAPI DDX_DateTimeCtrl(
    CDataExchange* pDX,  
    int nIDC,  
    COleDateTime& value);

void AFXAPI DDX_DateTimeCtrl(
    CDataExchange* pDX,  
    int nIDC,  
    CString& value);  
```  
  
### <a name="parameters"></a>Параметры  
 `pDX`  
 Указатель на [CDataExchange](../../mfc/reference/cdataexchange-class.md) объекта. Структура предоставляет этот объект для формирования контекста обмена данными, включая его направление. Удалить этот объект, не нужно.  
  
 `nIDC`  
 Идентификатор ресурса управления выбора даты и времени, связанные с переменной-члена.  
  
 *value*  
 В первых двух версий, ссылку на `CTime` или `COleDateTime` переменной-члена, диалоговое окно, представление формы или объект элемента управления представления обмене данными. В третьей версии ссылку на `CString` объект представления данных элемента управления.  
  
### <a name="remarks"></a>Примечания  
 Когда `DDX_DateTimeCtrl` вызывается, *значение* установлено равным текущему присваивается состояние даты и элементе управления или элемент управления *значение*, в зависимости от направления exchange.  
  
 В третьей версии выше `DDX_DateTimeCtrl` управляет передачей `CString` данные между датой время управления и [CString](../../atl-mfc-shared/reference/cstringt-class.md) элемент данных объекта представления элемента управления. Строка форматируется с использованием правил текущего языкового стандарта для форматирования дат и времени.  
  
 Дополнительные сведения об DDX см. в разделе [обмен данными диалоговых окон и проверка](../../mfc/dialog-data-exchange-and-validation.md).  
  
### <a name="requirements"></a>Требования  
  **Заголовок** afxdd_.h  
  
##  <a name="a-nameddxipaddressa--ddxipaddress"></a><a name="ddx_ipaddress"></a>DDX_IPAddress  
 `DDX_IPAddress` Функция управляет передачей данных между элементом управления IP-адрес и элемент данных объекта представления элемента управления.  
  
```  
void AFXAPI DDX_IPAddress(
    CDataExchange* pDX,  
    int nIDC,  
    DWORD& value);  
```  
  
### <a name="parameters"></a>Параметры  
 `pDX`  
 Указатель на объект `CDataExchange` . Структура предоставляет этот объект для формирования контекста обмена данными, включая его направление.  
  
 `nIDC`  
 Идентификатор ресурса управления IP-адрес, связанный со свойством элемента управления.  
  
 *value*  
 Ссылку на `DWORD` содержащий значение поля четыре элемента управления IP-адрес. Поля будут заполнены или следующим образом.  
  
|Поле|Биты, содержащий значение поля|  
|-----------|-------------------------------------|  
|3|от 0 до 7|  
|2|от 8 до 15|  
|1|16 до 23|  
|0|24 до 31|  
  
 Использование Win32 [IPM_GETADDRESS](http://msdn.microsoft.com/library/windows/desktop/bb761378) для чтения значение, или использовать [IPM_SETADDRESS](http://msdn.microsoft.com/library/windows/desktop/bb761380) для заполнения значения. Эти сообщения, описаны в [!INCLUDE[winSDK](../../atl/includes/winsdk_md.md)].  
  
### <a name="remarks"></a>Примечания  
 Когда `DDX_IPAddress` вызывается, *значение* считывается либо из элемента управления IP-адрес или *значение* записывается в элемент управления, в зависимости от направления exchange.  
  
 Дополнительные сведения об DDX см. в разделе [обмен данными диалоговых окон и проверка](../../mfc/dialog-data-exchange-and-validation.md).  
  
### <a name="requirements"></a>Требования  
  **Заголовок** afxdd_.h  
  
##  <a name="a-nameddxlbindexa--ddxlbindex"></a><a name="ddx_lbindex"></a>DDX_LBIndex  
 `DDX_LBIndex` Функция управляет передачей `int` данные между окно списка в диалоговом окне, форму, представление или объект элемента управления представления и `int` диалоговое окно, представление формы или объект элемента управления представления элемента данных.  
  
```  
void AFXAPI DDX_LBIndex(
    CDataExchange* pDX,  
    int nIDC,  
    int& index);  
```  
  
### <a name="parameters"></a>Параметры  
 `pDX`  
 Указатель на объект `CDataExchange` . Структура предоставляет этот объект для формирования контекста обмена данными, включая его направление.  
  
 `nIDC`  
 Идентификатор ресурса окно списка, связанного со свойством элемента управления.  
  
 *Индекс*  
 Ссылка на переменную-член диалоговое окно, представление формы или объект элемента управления представления обмене данными.  
  
### <a name="remarks"></a>Примечания  
 Когда `DDX_LBIndex` вызывается, *индекс* присваивается индекс текущего выделенного фрагмента поле списка. Если элемент не выбран, *индекс* задано значение -1.  
  
 Дополнительные сведения об DDX см. в разделе [обмен данными диалоговых окон и проверка](../../mfc/dialog-data-exchange-and-validation.md).  
  
### <a name="requirements"></a>Требования  
  **Заголовок** afxdd_.h  
  
##  <a name="a-nameddxlbstringa--ddxlbstring"></a><a name="ddx_lbstring"></a>DDX_LBString  
 `DDX_LBString` Функция управляет передачей `CString` данные между окно списка в диалоговом окне, форму, представление или объект элемента управления представления и `CString` диалоговое окно, представление формы или объект элемента управления представления элемента данных.  
  
```  
void AFXAPI DDX_LBString(
    CDataExchange* pDX,  
    int nIDC,  
    CString& value);  
```  
  
### <a name="parameters"></a>Параметры  
 `pDX`  
 Указатель на объект `CDataExchange` . Структура предоставляет этот объект для формирования контекста обмена данными, включая его направление.  
  
 `nIDC`  
 Идентификатор ресурса окно списка, связанного со свойством элемента управления.  
  
 *value*  
 Ссылка на переменную-член диалоговое окно, представление формы или объект элемента управления представления обмене данными.  
  
### <a name="remarks"></a>Примечания  
 Когда `DDX_LBString` вызывается для передачи данных в элемент управления списком, первый элемент в элементе управления, начало которого соответствует *значение* выбран. (Для сопоставления весь элемент, а не просто префикса, используйте [DDX_LBStringExact](#ddx_lbstringexact).) Если совпадений не найдено, элементы не выбраны. Соответствие без учета регистра.  
  
 Когда `DDX_LBString` вызывается для передачи данных из списка, *значение* присваивается текущее выделение поле списка. Если элемент не выбран, *значение* задана строка нулевой длины.  
  
> [!NOTE]
>  Если список имеет поле раскрывающегося списка, значение обмен ограничено до 255 символов.  
  
 Дополнительные сведения об DDX см. в разделе [обмен данными диалоговых окон и проверка](../../mfc/dialog-data-exchange-and-validation.md).  
  
### <a name="requirements"></a>Требования  
  **Заголовок** afxdd_.h  
  
##  <a name="a-nameddxlbstringexacta--ddxlbstringexact"></a><a name="ddx_lbstringexact"></a>DDX_LBStringExact  
 `DDX_CBStringExact` Функция управляет передачей `CString` данных между редактируемый элемент управления элемент управления списком в диалоговом окне, форму, представление или объект элемента управления представления и `CString` диалоговое окно, представление формы или объект элемента управления представления элемента данных.  
  
```  
void AFXAPI DDX_LBStringExact(
    CDataExchange* pDX,  
    int nIDC,  
    CString& value);  
```  
  
### <a name="parameters"></a>Параметры  
 `pDX`  
 Указатель на объект `CDataExchange` . Структура предоставляет этот объект для формирования контекста обмена данными, включая его направление.  
  
 `nIDC`  
 Идентификатор ресурса окно списка, связанного со свойством элемента управления.  
  
 *value*  
 Ссылка на переменную-член диалоговое окно, представление формы или объект элемента управления представления обмене данными.  
  
### <a name="remarks"></a>Примечания  
 Когда `DDX_LBStringExact` вызывается для передачи данных в элемент управления списком, первый элемент в элементе управления, который соответствует *значение* выбран. (Для сопоставления с префиксом, а не весь элемент, используйте [DDX_LBString](#ddx_lbstring).) Если совпадений не найдено, элементы не выбраны. Соответствие без учета регистра.  
  
 Когда `DDX_CBStringExact` вызывается для передачи данных из списка, *значение* присваивается текущее выделение поле списка. Если элемент не выбран, *значение* задана строка нулевой длины.  
  
> [!NOTE]
>  Если список имеет поле раскрывающегося списка, значение обмен ограничено до 255 символов.  
  
 Дополнительные сведения об DDX см. в разделе [обмен данными диалоговых окон и проверка](../../mfc/dialog-data-exchange-and-validation.md).  
  
### <a name="requirements"></a>Требования  
  **Заголовок** afxdd_.h  
  
##  <a name="a-nameddxmonthcalctrla--ddxmonthcalctrl"></a><a name="ddx_monthcalctrl"></a>DDX_MonthCalCtrl  
 `DDX_MonthCalCtrl` Функция управляет передачей даты между элемента управления календарем месяца ( [CMonthCalCtrl](../../mfc/reference/cmonthcalctrl-class.md)) в диалоговое окно, представление формы или элемента управления представления объекта и либо [CTime](../../atl-mfc-shared/reference/ctime-class.md) или [COleDateTime](../../atl-mfc-shared/reference/coledatetime-class.md) диалоговое окно, представление формы или объект элемента управления представления элемента данных.  
  
```  
void AFXAPI DDX_MonthCalCtrl(
    CDataExchange* pDX,  
    int nIDC,  
    CTime& value);

void AFXAPI DDX_MonthCalCtrl(
    CDataExchange* pDX,  
    int nIDC,  
    COleDateTime& value);  
```  
  
### <a name="parameters"></a>Параметры  
 `pDX`  
 Указатель на [CDataExchange](../../mfc/reference/cdataexchange-class.md) объекта. Структура предоставляет этот объект для формирования контекста обмена данными, включая его направление. Удалить этот объект, не нужно.  
  
 `nIDC`  
 Идентификатор ресурса для управления calendar month, связанный с переменной-члена.  
  
 *value*  
 Ссылку на `CTime` или `COleDateTime` переменной-члена диалоговое окно, представление формы или объект элемента управления представления обмене данными.  
  
### <a name="remarks"></a>Примечания  
  
> [!NOTE]
>  Элемент управления управляет только значения даты. Поля времени в объекте времени — набор с учетом времени создания окна элемента управления или было установлено некоторое время в элементе управления с помощью вызова `CMonthCalCtrl::SetCurSel`.  
  
 Когда `DDX_MonthCalCtrl` вызывается, *значение* присваивается текущее состояние элемента управления calendar month.  
  
 Дополнительные сведения об DDX см. в разделе [обмен данными диалоговых окон и проверка](../../mfc/dialog-data-exchange-and-validation.md).  
  
### <a name="requirements"></a>Требования  
  **Заголовок** afxdd_.h  
  
##  <a name="a-nameddxradioa--ddxradio"></a><a name="ddx_radio"></a>DDX_Radio  
 `DDX_Radio` Функция управляет передачей `int` данных между группой управления переключателей в диалоговом окне, форму, представление или объект элемента управления представления и `int` диалоговое окно, представление формы или объект элемента управления представления элемента данных. Значение `int` данные-член определяется в соответствии с какой переключатель была нажата кнопка внутри группы.  
  
```  
void AFXAPI DDX_Radio(
    CDataExchange* pDX,  
    int nIDC,  
    int& value);  
```  
  
### <a name="parameters"></a>Параметры  
 `pDX`  
 Указатель на объект `CDataExchange` . Структура предоставляет этот объект для формирования контекста обмена данными, включая его направление.  
  
 `nIDC`  
 Идентификатор ресурса первый переключатель в группе.  
  
 *value*  
 Ссылка на переменную-член диалоговое окно, представление формы или объект элемента управления представления обмене данными.  
  
### <a name="remarks"></a>Примечания  
 Когда `DDX_Radio` вызывается, *значение* присваивается текущее состояние элемента управления группы переключателей. Отсчитываемый от нуля индекс переключатель, который выбран в данный момент задано значение или -1, если элементы управления радио не проверяются.  
  
 Например, в случае, первый переключатель в группе проверяется значение (кнопка со стилем WS_GROUP) `int` член является 0, и т. д.  
  
 Дополнительные сведения об DDX см. в разделе [обмен данными диалоговых окон и проверка](../../mfc/dialog-data-exchange-and-validation.md).  
  
### <a name="requirements"></a>Требования  
  **Заголовок** afxdd_.h  
  
##  <a name="a-nameddxscrolla--ddxscroll"></a><a name="ddx_scroll"></a>DDX_Scroll  
 `DDX_Scroll` Функция управляет передачей `int` данных между полосы прокрутки элемента управления в диалоговом окне, форму, представление или объект элемента управления представления и `int` диалоговое окно, представление формы или объект элемента управления представления элемента данных.  
  
```  
void AFXAPI DDX_Scroll(
    CDataExchange* pDX,  
    int nIDC,  
    int& value);  
```  
  
### <a name="parameters"></a>Параметры  
 `pDX`  
 Указатель на объект `CDataExchange` . Структура предоставляет этот объект для формирования контекста обмена данными, включая его направление.  
  
 `nIDC`  
 Идентификатор ресурса управления полосой прокрутки, связанный со свойством элемента управления.  
  
 *value*  
 Ссылка на переменную-член диалогового окна, представления формы или объекта представления элемента управления, с которой осуществляется обмен данными.  
  
### <a name="remarks"></a>Примечания  
 Когда `DDX_Scroll` вызывается, *значение* задать текущее положение ползунка полосы прокрутки элемента управления. Дополнительные сведения о значениях, связанных с текущей позиции элемента управления thumb см. в разделе [GetScrollPos](http://msdn.microsoft.com/library/windows/desktop/bb787585) в [!INCLUDE[winSDK](../../atl/includes/winsdk_md.md)].  
  
 Дополнительные сведения об DDX см. в разделе [обмен данными диалоговых окон и проверка](../../mfc/dialog-data-exchange-and-validation.md).  
  
### <a name="requirements"></a>Требования  
  **Заголовок** afxdd_.h  
  
##  <a name="a-nameddxslidera--ddxslider"></a><a name="ddx_slider"></a>DDX_Slider  
 `DDX_Slider` Функция управляет передачей `int` данные между ползунок в представлении формы или диалогового окна и `int` элемент данных объекта представление формы или диалогового окна.  
  
```  
void AFXAPI DDX_Slider(
    CDataExchange* pDX,  
    int nIDC,  
    int& value);  
```  
  
### <a name="parameters"></a>Параметры  
 `pDX`  
 Указатель на [CDataExchange](../../mfc/reference/cdataexchange-class.md) объекта. Структура предоставляет этот объект для формирования контекста обмена данными, включая его направление.  
  
 `nIDC`  
 Идентификатор ресурса ползунком.  
  
 *value*  
 Ссылка на значение для обмена. Этот параметр содержит или задает текущее положение ползунка.  
  
### <a name="remarks"></a>Примечания  
 Когда `DDX_Slider` вызывается, *значение* задать текущее положение ползунка полосы прокрутки элемента управления, или получает значение позиции, в зависимости от направления exchange.  
  
 Дополнительные сведения об DDX см. в разделе [обмен данными диалоговых окон и проверка](../../mfc/dialog-data-exchange-and-validation.md). Сведения об элементах управления "ползунок" в разделе [CSliderCtrl с помощью](../../mfc/using-csliderctrl.md).  
  
### <a name="requirements"></a>Требования  
  **Заголовок** afxdd_.h  
  
##  <a name="a-nameddxtexta--ddxtext"></a><a name="ddx_text"></a>DDX_Text  
 `DDX_Text` Функция управляет передачей `int`, **UINT**, **длинные**, `DWORD`, `CString`, **float**, или **двойные** данными между элемент управления редактированием в диалоговое окно, представление формы и представление управления и [CString](../../atl-mfc-shared/reference/cstringt-class.md) диалоговое окно, представление формы или объект элемента управления представления элемента данных.  
  
```  
void AFXAPI DDX_Text(
    CDataExchange* pDX,  
    int nIDC,  
    BYTE& value);

void AFXAPI DDX_Text(
    CDataExchange* pDX,  
    int nIDC,  
    short& value);

void AFXAPI DDX_Text(
    CDataExchange* pDX,  
    int nIDC,  
    int& value);

void AFXAPI DDX_Text(
    CDataExchange* pDX,  
    int nIDC,  
    UINT& value);

void AFXAPI DDX_Text(
    CDataExchange* pDX,  
    int nIDC,  
    long& value);

void AFXAPI DDX_Text(
    CDataExchange* pDX,  
    int nIDC,  
    DWORD& value);

void AFXAPI DDX_Text(
    CDataExchange* pDX,  
    int nIDC,  
    CString& value);

void AFXAPI DDX_Text(
    CDataExchange* pDX,  
    int nIDC,  
    float& value);

void AFXAPI DDX_Text(
    CDataExchange* pDX,  
    int nIDC,  
    double& value);

void AFXAPI DDX_Text(
    CDataExchange* pDX,  
    int nIDC,  
    COleCurrency& value);

void AFXAPI DDX_Text(
    CDataExchange* pDX,  
    int nIDC,  
    COleDateTime& value);  
```  
  
### <a name="parameters"></a>Параметры  
 `pDX`  
 Указатель на [CDataExchange](../../mfc/reference/cdataexchange-class.md) объекта. Структура предоставляет этот объект для формирования контекста обмена данными, включая его направление.  
  
 `nIDC`  
 Идентификатор элемента управления редактированием в диалоговое окно, представление формы или объект элемента управления представления.  
  
 *value*  
 Ссылка на член данных в диалоговое окно, представление формы или элемента управления представления объекта. Тип данных *значение* зависит от того, на котором перегруженные версии `DDX_Text` использовании.  
  
### <a name="remarks"></a>Примечания  
 Дополнительные сведения об DDX см. в разделе [обмен данными диалоговых окон и проверка](../../mfc/dialog-data-exchange-and-validation.md).  

### <a name="requirements"></a>Требования  
  **Заголовок** afxdd_.h  

## <a name="see-also"></a>См. также  
 [Стандартные процедуры проверки данных](../../mfc/reference/standard-dialog-data-validation-routines.md)   
 [Макросы и глобальные объекты](../../mfc/reference/mfc-macros-and-globals.md)

